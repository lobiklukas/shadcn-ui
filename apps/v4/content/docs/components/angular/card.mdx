---
title: Card
description: Displays a card with header, content, and footer.
base: angular
component: true
---

<ComponentPreview
  framework="angular"
  name="card-demo"
  previewClassName="h-[30rem]"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-angular/card
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource framework="angular" name="card" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```ts
import {
  CardActionComponent,
  CardComponent,
  CardContentComponent,
  CardDescriptionComponent,
  CardFooterComponent,
  CardHeaderComponent,
  CardTitleComponent,
} from "@/components/ui/card"
```

```html
<div uiCard>
  <div uiCardHeader>
    <h3 uiCardTitle>Title</h3>
    <p uiCardDescription>Description</p>
    <div uiCardAction><!-- action --></div>
  </div>
  <div uiCardContent><!-- content --></div>
  <div uiCardFooter><!-- footer --></div>
</div>
```

Use a real heading element for `uiCardTitle` so the page heading hierarchy is correct for screen readers.

## Composition

```text
[uiCard]
├── [uiCardHeader]
│   ├── [uiCardTitle]
│   ├── [uiCardDescription]
│   └── [uiCardAction]
├── [uiCardContent]
└── [uiCardFooter]
```

## Size

Use the `size="sm"` input to set the size of the card to small. The small size variant uses smaller spacing.

<ComponentPreview framework="angular" name="card-small" previewClassName="h-96" />

## Spacing

In addition to the `size` input, you can use the `--card-spacing` CSS variable to control the spacing between sections and the inset of card parts.

<ComponentPreview framework="angular" name="card-spacing" previewClassName="h-[34rem]" />

Use negative margins with `-mx-(--card-spacing)` to make content go edge to edge while keeping it aligned with the card inset. When the edge-to-edge content sits above a footer, use `-mb-(--card-spacing)` on `[uiCardContent]` to remove the section gap.

<ComponentPreview framework="angular" name="card-edge-to-edge" previewClassName="h-[24rem]" />

## Image

Add an image before the card header to create a card with an image.

<ComponentPreview framework="angular" name="card-image" previewClassName="h-[32rem]" />

## RTL

To enable RTL support in Force UI, see the [RTL configuration guide](/docs/rtl).

<ComponentPreview framework="angular" name="card-rtl" direction="rtl" previewClassName="h-[30rem]" />

## API Reference

### CardComponent

| Input   | Type                | Default     |
| ------- | ------------------- | ----------- |
| `size`  | `"default" \| "sm"` | `"default"` |
| `class` | `string`            | –           |

### CardHeaderComponent · CardTitleComponent · CardDescriptionComponent · CardActionComponent · CardContentComponent · CardFooterComponent

| Input   | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | –       |
