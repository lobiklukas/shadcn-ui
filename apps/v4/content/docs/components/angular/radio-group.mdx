---
title: Radio Group
description: A set of checkable buttons—known as radio buttons—where no more than one of the buttons can be checked at a time.
base: angular
component: true
links:
  doc: https://radix-ng.com/primitives/radio-group
  api: https://radix-ng.com/primitives/radio-group
---

<ComponentPreview framework="angular" name="radio-group-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-angular/radio-group
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash
npm install @radix-ng/primitives
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource framework="angular" name="radio-group" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```ts showLineNumbers
import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group"
```

```html showLineNumbers
<div uiRadioGroup defaultValue="option-1">
  <div class="flex items-center gap-2">
    <button uiRadioGroupItem id="r1" value="option-1"></button>
    <label uiLabel for="r1">Default</label>
  </div>
</div>
```

## Composition

Use the following composition to build a `RadioGroup`:

```text
RadioGroup
├── RadioGroupItem
└── RadioGroupItem
```

## RTL

To enable RTL support in Force UI, see the [RTL configuration guide](/docs/rtl).

<Callout>
A dedicated `radio-group-rtl` Angular example has not been ported yet. It is tracked in the example-parity report.
</Callout>

## API Reference

See the [radix-ng](https://radix-ng.com/primitives/radio-group) documentation for more information.
