---
title: Dialog
description: A window overlaid on either the primary window or another dialog window, rendering the content underneath inert.
featured: true
base: angular
component: true
links:
  doc: https://radix-ng.com/primitives/dialog
  api: https://radix-ng.com/primitives/dialog
---

<ComponentPreview framework="angular" name="dialog-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-angular/dialog
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash
npm install @radix-ng/primitives class-variance-authority clsx tailwind-merge
```

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource framework="angular" name="dialog" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```ts showLineNumbers
import {
  DialogClose,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogPortal,
  DialogRoot,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog"
```

```html showLineNumbers
<div uiDialogRoot>
  <button uiButton variant="outline" uiDialogTrigger type="button">Open</button>
  <ng-template uiDialogPortal>
    <div uiDialogOverlay></div>
    <div uiDialogContent class="sm:max-w-sm">
      <div uiDialogHeader>
        <h2 uiDialogTitle>Edit profile</h2>
        <p uiDialogDescription>Make changes to your profile.</p>
      </div>
      <div uiDialogFooter>
        <button uiButton variant="outline" type="button" uiDialogClose>Cancel</button>
      </div>
    </div>
  </ng-template>
</div>
```

Every part is an attribute selector on a native element, so the host keeps its
native semantics — Angular's idiomatic equivalent of React's `asChild`/`render`.

## Composition

Use the following composition to build a `Dialog`:

```text
DialogRoot
├── DialogTrigger
└── DialogPortal (ng-template)
    ├── DialogOverlay
    └── DialogContent
        ├── DialogHeader
        │   ├── DialogTitle
        │   └── DialogDescription
        └── DialogFooter
            └── DialogClose
```

## Edit profile

A dialog with a form: two fields in a field group, a cancel action bound to `uiDialogClose`, and a submit button.

<ComponentPreview framework="angular" name="dialog-demo" />

## Close button

The built-in top-right close button is rendered by default. The example composes an input with an explicit footer close action.

<ComponentPreview framework="angular" name="dialog-close-button" />

## No close button

Set `[showCloseButton]="false"` to remove the built-in top-right close button.

<ComponentPreview framework="angular" name="dialog-no-close-button" />

## Scrollable content

Scroll long content inside the dialog body with `max-h-[50vh] overflow-y-auto`.

<ComponentPreview framework="angular" name="dialog-scrollable-content" />

## Sticky footer

Combine the scrollable body with a footer that stays visible while the content scrolls.

<ComponentPreview framework="angular" name="dialog-sticky-footer" />

## RTL

To enable RTL support in Force UI, see the [RTL configuration guide](/docs/rtl).

<ComponentPreview framework="angular" name="dialog-rtl" direction="rtl" />

## API Reference

See the [radix-ng dialog](https://radix-ng.com/primitives/dialog) documentation for more information.
