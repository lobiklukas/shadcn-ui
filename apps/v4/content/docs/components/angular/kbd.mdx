---
title: Kbd
description: Used to display textual user input from keyboard.
base: angular
component: true
---

<ComponentPreview framework="angular" name="kbd-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-angular/kbd
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource framework="angular" name="kbd" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```ts
import { KbdComponent, KbdGroupComponent } from "@/components/ui/kbd"
```

```html
<kbd uiKbd>Ctrl</kbd>
```

## Composition

```text
[uiKbdGroup]
├── [uiKbd]
└── [uiKbd]
```

## Group

Wrap multiple `Kbd` parts in a `KbdGroup` when they render as a unit.

<ComponentPreview framework="angular" name="kbd-group" />

## Button

Use a `Kbd` inside a button to surface its shortcut inline.

<ComponentPreview framework="angular" name="kbd-button" />

## Tooltip

Show the shortcut inside a tooltip triggered from a `ButtonGroup`.

<ComponentPreview framework="angular" name="kbd-tooltip" />

## Input Group

Combine a `Kbd` pair with an input group addon to document a search shortcut.

<ComponentPreview framework="angular" name="kbd-input-group" />

### Primary

Use `variant="primary"` when placing a `Kbd` on a solid or brand surface.

<ComponentPreview framework="angular" name="kbd-primary" />

## RTL

To enable RTL support in Force UI, see the [RTL configuration guide](/docs/rtl).

<ComponentPreview framework="angular" name="kbd-rtl" direction="rtl" />

## API Reference

### KbdComponent

| Input     | Type                     | Default     |
| --------- | ------------------------ | ----------- |
| `variant` | `"default" \| "primary"` | `"default"` |
| `class`   | `string`                 | –           |

### KbdGroupComponent

| Input   | Type     | Default |
| ------- | -------- | ------- |
| `class` | `string` | –       |
