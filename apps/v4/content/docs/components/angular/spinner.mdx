---
title: Spinner
description: An indicator that can be used to show a loading state.
base: angular
component: true
---

<ComponentPreview framework="angular" name="spinner-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-angular/spinner
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource framework="angular" name="spinner" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```ts
import { SpinnerComponent } from "@/components/ui/spinner"
```

```html
<span uiSpinner></span> <span uiSpinner color="primary" size="lg"></span>
```

The spinner is `aria-hidden` — it is purely visual. The container owns the loading state via `aria-busy="true"` + an `aria-live` region.

The `animate-spinner` animation (500ms) requires this in your `tailwind.css`:

```css
@theme {
  --animate-spinner: spin 500ms linear infinite;
}
```

## Colors

<ComponentPreview framework="angular" name="spinner-colors" />

## Sizes

<ComponentPreview framework="angular" name="spinner-sizes" />

## Size

Use the `size` utility class for arbitrary spinner dimensions.

<ComponentPreview framework="angular" name="spinner-size" />

## Button

<ComponentPreview framework="angular" name="spinner-button" />

## Badge

<ComponentPreview framework="angular" name="spinner-badge" />

## Input Group

<ComponentPreview framework="angular" name="spinner-input-group" />

## Empty

<ComponentPreview
  framework="angular"
  name="spinner-empty"
  previewClassName="h-96 p-0"
/>

## RTL

To enable RTL support in Force UI, see the [RTL configuration guide](/docs/rtl).

<ComponentPreview framework="angular" name="spinner-rtl" direction="rtl" />

## API Reference

### SpinnerComponent

| Input   | Type                                                 | Default     |
| ------- | ---------------------------------------------------- | ----------- |
| `color` | `"default" \| "primary" \| "onPrimary" \| "inherit"` | `"default"` |
| `size`  | `"xs" \| "sm" \| "md" \| "lg"`                       | `"sm"`      |
| `class` | `string`                                             | –           |
