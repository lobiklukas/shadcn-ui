---
title: Progress
description: Displays an indicator showing the completion progress of a task.
base: angular
component: true
---

<ComponentPreview framework="angular" name="progress-demo" />

## Installation

<CodeTabs>
<TabsList><TabsTrigger value="cli">Command</TabsTrigger><TabsTrigger value="manual">Manual</TabsTrigger></TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @force-ui-angular/progress
```

</TabsContent>
<TabsContent value="manual">
<Steps className="mb-0 pt-2">
<Step>Copy and paste the following code into your project.</Step>
<ComponentSource framework="angular" name="progress" />
<Step>Update the import paths to match your project setup.</Step>
</Steps>
</TabsContent>
</CodeTabs>

## Usage

```ts
import { Progress } from "@/components/ui/progress"
```

```html
<div uiProgress [value]="75" aria-label="Uploading (75%)"></div>
```

Always supply `aria-label` or `aria-labelledby` — the component cannot derive an accessible name from context. Pass `[value]="null"` for an indeterminate (pulsing) state.

## Indeterminate

Pass `[value]="null"` for an indeterminate (pulsing) state.

<ComponentPreview framework="angular" name="progress-indeterminate" />

## Label

<Callout>
  The Angular progress is a single-part bar — the React `ProgressLabel` /
  `ProgressValue` parts don't exist, so the label and percentage render in a
  header row around the bar. The rendered result matches the React example.
</Callout>

Render a label and the current value above the bar.

<ComponentPreview framework="angular" name="progress-label" />

## Controlled

Bind `value` to a signal and drive it from a slider.

<ComponentPreview framework="angular" name="progress-controlled" />

## RTL

To enable RTL support in Force UI, see the [RTL configuration guide](/docs/rtl).

<ComponentPreview framework="angular" name="progress-rtl" />

## API Reference

### ProgressComponent

| Input   | Type             | Default |
| ------- | ---------------- | ------- |
| `value` | `number \| null` | –       |
| `max`   | `number`         | `100`   |
| `class` | `string`         | –       |
