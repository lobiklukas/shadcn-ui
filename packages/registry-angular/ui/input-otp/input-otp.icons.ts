/**
 * InputOTPSeparator icon — single swap point.
 *
 * Raw inline SVG from `@material-symbols/svg-400` (Material Symbols,
 * Rounded style, outline = FILL 0), injected as a real `<svg>` through
 * `[innerHTML]` (sanitizer-trusted). Maps the registry's `MinusIcon` by
 * meaning. To swap the icon, change this file only.
 */
export const INPUT_OTP_SEPARATOR_SVG = `<svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 -960 960 960"><path d="M200-440v-80h560v80H200Z"/></svg>`
